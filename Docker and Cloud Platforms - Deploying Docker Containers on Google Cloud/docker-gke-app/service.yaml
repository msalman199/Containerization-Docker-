apiVersion: v1
kind: Service
metadata:
  name: docker-gke-app-service
  labels:
    app: docker-gke-app
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
  selector:
    app: docker-gke-app
